# ──────────────────────────────────────────────
# EcoWeatherFit — Python Dependencies
# ──────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────

# Core data handling
pandas>=2.0,<3.0
numpy>=1.24,<3.0

# Machine Learning
scikit-learn>=1.3,<2.0
tensorflow==2.18.0          # Hard-pin: Keras 3 serialization (quantization_config) is not compatible across 2.15 ↔ 2.16+

# Visualisation
matplotlib>=3.7,<4.0
plotly>=5.18,<6.0

# Weather data sources
meteostat>=1.6,<2.0         # Historical weather data (Meteostat Python lib)
requests>=2.31,<3.0         # HTTP client for OpenWeatherMap API

# Google Gemini GenAI
google-generativeai>=0.4,<1.0

# Web UI
streamlit>=1.30,<2.0
extra-streamlit-components>=0.1.71   # Browser-cookie manager for JWT session persistence

# Authentication & persistence
SQLAlchemy>=2.0,<3.0        # ORM for the user / preferences / wardrobe / history tables
passlib[bcrypt]>=1.7,<2.0   # Password hashing wrapper
bcrypt<4.1                   # Pinned: bcrypt 4.1+ raises a hard error on >72-byte inputs that passlib's
                             # self-test sends during scheme detection — keeps passlib's bcrypt backend usable.
PyJWT>=2.8,<3.0             # JWT session tokens (HS256, 7-day TTL)
email-validator>=2.0,<3.0   # Robust email format validation at signup

# Testing
pytest>=7.4,<9.0

# Utilities
python-dotenv>=1.0,<2.0     # Load .env file for API keys
